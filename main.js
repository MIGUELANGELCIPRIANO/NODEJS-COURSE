const web = require('./module/module')

console.log(web)

const { address1, address2, user } = require('./module/module')

console.log(user)

const math = require('./math/math')

console.log(math.add(10, 20))
